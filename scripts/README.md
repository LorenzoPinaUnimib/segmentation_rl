# Documentazione pipeline

La **relazione tecnica completa** (architettura, flussi, I/O, metriche, configurazione) si trova nel README principale del repository:

**[../README.md](../README.md)**

Per eseguire il pipeline:

```bash
python scripts/run_full_pipeline.py
```
